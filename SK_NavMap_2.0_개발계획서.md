# SK 항법지도 2.0 — 개발 계획서

작성일: 2026-07-16  
버전: v0.1 (논의용)

---

## 1. 프로그램 개요

### 목적
헬리콥터 조종사 3명이 항법 경로 계획 및 확인을 위해
모바일(휴대폰)에서 언제 어디서나 쉽게 사용할 수 있는 항법 지도 앱

### 핵심 원칙
- **모바일 퍼스트** — 처음부터 휴대폰 기준으로 설계
- **지도 중심** — 지도가 항상 화면의 90% 이상 차지
- **최소 클릭** — 원하는 정보까지 2번 이내 터치
- **조종사 친화** — FMS 좌표, 연료 계산 등 항공 특화

### 사용 환경
- 주 기기: 스마트폰 (삼성 갤럭시 등 Android, iOS)
- 보조: PC/태블릿
- 인터넷: 온라인 필수 (지도 로딩)
- 시점: 비행 전 브리핑 및 경로 계획

---

## 2. 기술 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| 지도 | 구글 맵스 API | 위성 품질 최고, 기존 API키 보유 |
| 호스팅 | GitHub Pages | 무료, 이미 GitHub 보유 |
| 앱 형태 | PWA | 바탕화면 설치, 배너 없음 |
| 데이터 | 구글 시트 API | 기존 DB 그대로 활용 |
| 프론트엔드 | HTML/CSS/JS | 별도 프레임워크 없이 단순하게 |
| 비용 | $0 | 구글 맵스 무료 크레딧 (3명 충분) |

---

## 3. 화면 구성 (미리보기 기준)

### 3-1. 메인 지도 화면 (항상 표시)
```
┌─────────────────────────┐
│ 🚁 SK항법지도    [레이어]│  ← 상단바 (최소화)
├─────────────────────────┤
│                         │
│   [거리] [130KTS] [연료] │  ← 결과칩 (경로 선택 시만 표시)
│                         │
│      구글 위성지도       │
│                         │
│   H(빨강) H(초록) 마커  │
│   오렌지 경로 라인       │
│                         │
├─────────────────────────┤
│ [🗂️ 항법경로 선택    >] [+]│  ← 하단 액션바
└─────────────────────────┘
```

**동작:**
- 마커(H) 클릭 → 착륙장 정보 시트 (하단에서 올라옴)
- 하단 바 클릭 → 경로 검색 시트
- [+] 클릭 → 추가 메뉴
- 결과칩: 경로 선택 전까지 숨김

### 3-2. 경로 검색 시트 (하단에서 올라옴)
```
┌─────────────────────────┐
│ —— (핸들)               │
│ 항법경로 검색           │
│ [🔍 경로명, 출발지 검색] │
│ [전체][⭐즐겨찾기][만남출발][김포출발] │
│                         │
│ ✈️ MANN-SKCM 만남-울산  │
│    만남의광장 → 울산     178.4NM  82.3분  ⭐ │
│                         │
│ ✈️ MANN-ADSKC 만남-안동 │
│    만남의광장 → SK바이오  108.7NM  50.2분  ☆ │
│ ...                     │
└─────────────────────────┘
```

**동작:**
- 탭으로 필터링 (전체 / 즐겨찾기 / 출발지별)
- 텍스트 검색 (경로명, 출발지, 도착지)
- 카드 클릭 → 지도에 오렌지 경로 표시 + 결과칩 표시
- ⭐ 클릭 → 즐겨찾기 등록/해제 (브라우저 저장)

### 3-3. 착륙장 정보 시트 (마커 클릭 시)
```
┌─────────────────────────┐
│ —— (핸들)           [✕] │
│ 만남의 광장 헬기장       │
│ SK착륙장                │
│ LAT N3727.69  LNG E12702.43  [📋복사] │
│ ─────────────────────── │
│ 관련 항법경로            │
│ [→울산 82.3분] [→안동 50.2분] [→대덕 30.1분] │
└─────────────────────────┘
```

**동작:**
- FMS 좌표 표시 (도분 형식)
- 복사 버튼 → 클립보드 복사
- 관련 경로 칩 → 가로 스크롤
- 경로 칩 클릭 → 해당 경로 지도에 표시

### 3-4. 추가 메뉴 (+ 버튼)
```
┌─────────────────────────┐
│ —— (핸들)               │
│ 추가하기                │
│ ✈️ 새 항법경로           │
│    출발~경유~도착 경로 작성 │
│ 🅗 착륙장 추가           │
│    새 헬기장/착륙지점     │
│ 📍 WayPoint 추가         │
│    항법 경유점 등록       │
└─────────────────────────┘
```

---

## 4. 주요 기능 목록

### 4-1. 조회 기능 (Read)
| 기능 | 설명 |
|---|---|
| 착륙장 마커 표시 | SK착륙장(빨강H), 착륙장(초록H) |
| WayPoint 표시 | 파란 점 (레이어 ON시) |
| 항법경로 라인 | 초록 라인 (레이어 ON시) |
| 경로 선택 강조 | 오렌지 실선 |
| DB 없는 경로 | 오렌지 점선 + 참고값(~) |
| FMS 좌표 | 마커 클릭 시 도분 형식 표시 |
| 거리/시간/연료 | 경로 선택 시 자동 계산 |
| 즐겨찾기 | 브라우저 로컬 저장 |
| 레이어 ON/OFF | 마커/경로 표시 제어 |

### 4-2. 입력 기능 (Write)
| 기능 | 설명 |
|---|---|
| 착륙장 추가 | 이름/좌표 입력 → 구글 시트 저장 |
| WayPoint 추가 | 이름/좌표 입력 → 구글 시트 저장 |
| 항법경로 추가 | 지도 탭으로 경유점 → 구글 시트 저장 |
| 경유점 드래그 | 드래그로 위치 조정 |
| 수정/삭제 | 경로 목록에서 수정/삭제 |

### 4-3. 계산 기능
| 기능 | 공식 |
|---|---|
| 거리(NM) | Haversine 공식 (대권 거리) |
| 130KTS 소요시간 | 거리 ÷ 130 × 60 (분) |
| 140KTS 소요시간 | 거리 ÷ 140 × 60 (분) |
| 연료(LBS) | 130KTS 시간 × 10 LBS/분 |
| FMS 좌표 변환 | 소수점 DD → 도분 DDM |

---

## 5. 데이터 구조

### 구글 시트 (기존 유지)
```
항법지도_DB
├── SK착륙장  : name | lat | lng | memo
├── 착륙장    : name | lat | lng | memo
├── WayPoint  : name | lat | lng | memo
├── 항법경로  : name | dep | arr | (빈칸) | coords
├── 소요시간  : name | dep | arr | dist_nm | min_130 | min_140 | fuel_lbs
└── 설정      : Maps_API_Key | ...
```

### 데이터 흐름
```
구글 시트
    ↓ (앱 시작 시 1회 로드)
구글 Apps Script API
    ↓
브라우저 메모리 (JS 변수)
    ↓
지도/UI 렌더링
    ↓
사용자 조작 (추가/수정/삭제)
    ↓
구글 Apps Script API
    ↓
구글 시트 업데이트
```

---

## 6. 개발 단계별 플로우

### Phase 0 — 준비 (1~2일)
```
□ GitHub 저장소 생성 (sk-navmap)
□ GitHub Pages 활성화
□ 구글 Apps Script 새 프로젝트 생성
□ API 엔드포인트 설계
□ 폴더 구조 설계
```

### Phase 1 — 기반 구축 (3~5일)
```
□ index.html 기본 구조
□ 구글 맵스 API 연동 (위성지도)
□ PWA 설정 (manifest.json, service worker)
□ 모바일 기본 레이아웃
□ 구글 시트 데이터 로드
```

### Phase 2 — 조회 기능 (5~7일)
```
□ 착륙장 마커 표시 (SK/일반)
□ WayPoint 마커 표시
□ 항법경로 라인 표시
□ 레이어 ON/OFF
□ 경로 검색 시트 UI
□ 경로 선택 → 오렌지 강조
□ 거리/시간/연료 계산
□ 마커 클릭 → 착륙장 정보 시트
□ FMS 좌표 변환/복사
□ 즐겨찾기 (localStorage)
```

### Phase 3 — 입력 기능 (5~7일)
```
□ + 추가 메뉴
□ 착륙장 추가 (폼 → 시트 저장)
□ WayPoint 추가
□ 항법경로 추가 (지도 탭)
□ 경유점 드래그 수정
□ 수정/삭제
```

### Phase 4 — 완성도 (3~5일)
```
□ 모바일 UX 최적화
□ 로딩/에러 처리
□ 오프라인 캐싱 (Service Worker)
□ 바탕화면 설치 안내
□ 전체 테스트
□ GitHub Pages 배포
```

---

## 7. 파일 구조

```
sk-navmap/
├── index.html          ← 메인 앱
├── manifest.json       ← PWA 설정
├── sw.js               ← Service Worker (오프라인)
├── css/
│   └── style.css       ← 스타일
├── js/
│   ├── app.js          ← 메인 로직
│   ├── map.js          ← 지도 관련
│   ├── data.js         ← 데이터 로드/저장
│   ├── ui.js           ← UI 컴포넌트
│   └── calc.js         ← 거리/연료 계산
├── icons/
│   ├── icon-192.png    ← PWA 아이콘
│   └── icon-512.png
└── README.md
```

---

## 8. 현재 시스템과 차이점

| 항목 | 현재 (앱스크립트) | 신규 (GitHub Pages) |
|---|---|---|
| 구글 배너 | ❌ 항상 표시 | ✅ 없음 |
| 모바일 최적화 | 패치 방식 | ✅ 처음부터 설계 |
| 바탕화면 설치 | 어려움 | ✅ PWA |
| 로딩 속도 | 느림 | ✅ 빠름 |
| 오프라인 | ❌ | ✅ 캐싱 |
| 지도 | 구글 위성 | ✅ 구글 위성 동일 |
| 데이터 | 구글 시트 | ✅ 구글 시트 동일 |
| 기능 | 기존 | ✅ 동일 + 개선 |
| 비용 | $0 | $0 |

---

## 9. 논의 필요 사항

### 9-1. 데이터 연동 방식 결정 필요
```
A안) 구글 시트 + Apps Script API 유지
     - 장점: 데이터 관리 현행 유지
     - 단점: Apps Script 별도 배포 필요
     - 추천: 기존 Code.gs 그대로 재활용 가능

B안) JSON 파일로 변환해서 GitHub에 저장
     - 장점: 빠름, 안정적, 오프라인 완전 지원
     - 단점: 데이터 수정 시 JSON 업데이트 필요
     - 추천: 월 10~20회 수정이면 충분히 관리 가능
```

### 9-2. 탭 필터 구성 (경로 검색)
```
현재 미리보기: [전체][즐겨찾기][만남출발][김포출발]
→ 출발지 탭을 어떻게 구성할지?
   - 고정: 만남 / 김포 / 인천
   - 동적: 자주 쓰는 출발지 자동 생성
```

### 9-3. 경로 추가 방식
```
현재: 지도 클릭으로 경유점 추가
→ 모바일에서 정밀한 탭이 어려울 수 있음
   A안: 지도 탭 + 드래그 (현재 방식)
   B안: WPT 코드 검색으로 경유점 추가
   C안: 두 가지 병행
```

### 9-4. 수정/삭제 권한
```
현재: 관리자 버튼 ON/OFF
→ 신규에서는?
   A안: 동일하게 관리자 버튼
   B안: 비밀번호 입력
   C안: 권한 없이 모두 수정 가능
```

---

## 10. 예상 일정

| 단계 | 기간 | 내용 |
|---|---|---|
| Phase 0 | 1~2일 | 환경 준비 |
| Phase 1 | 3~5일 | 기반 구축 |
| Phase 2 | 5~7일 | 조회 기능 |
| Phase 3 | 5~7일 | 입력 기능 |
| Phase 4 | 3~5일 | 완성도 |
| **합계** | **약 3~4주** | |

---

*이 문서는 논의 후 수정됩니다.*
